import React from "react";
import { HashRouter } from "react-router-dom";
import AppWrapper from "./AppWrapper";

function App() {
  return (
    <HashRouter>
      <AppWrapper />
    </HashRouter>
  );
}

export default App;
